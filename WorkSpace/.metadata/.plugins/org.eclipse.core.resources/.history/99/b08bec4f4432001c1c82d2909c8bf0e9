package test_array;

public class BookDAO {
	void display(BookDTO list) {
		System.out.println("책의 제목\t출판사\t가격\t수량\t저자");
		for (BookDTO BookDTO : list) {
			System.out.println(list.getName()+"\t"+list.getPublish()+"\t"+list.getPrice()+"\t"+list.getCnt()+"\t"+list.getWriiter());
		}
	}
}
